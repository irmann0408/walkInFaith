package com.bibleadventures.ui.navigation

/**
 * Identifies a Main Menu button independent of its display label, so
 * navigation dispatch never depends on translatable UI text.
 */
enum class MenuItemId {
    ADVENTURES,
    BADGES,
    SCRIPTURE_CARDS,
    CHARACTER,
    SETTINGS,
    PARENT_AREA,
}
